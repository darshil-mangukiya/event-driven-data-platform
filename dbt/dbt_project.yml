name: cloud_scale_data_platform
version: "1.0.0"
config-version: 2

profile: cloud_scale_data_platform

model-paths: ["models"]
test-paths: ["tests"]

models:
  cloud_scale_data_platform:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: mart

